name: Go build
on:
  push:
    branches:
      - "integration/main"
      - "stable*"
  pull_request:
    branches:
      - "integration/main"
      - "stable*"

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.21'

      - name: Build
        run: |
          export GOFLAGS=-buildvcs=false
          go mod tidy
          go build -v -o .
